<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ProyectoPasantía" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:colorBackground">@android:color/white</item>
    </style>

    <style name="Theme.ProyectoPasantía" parent="Base.Theme.ProyectoPasantía" />
</resources>
